# Enable AndroidX support (Fixes the build error)
android.useAndroidX=true
android.enableJetifier=true

# JVM memory settings to prevent out-of-memory errors on the builder
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
